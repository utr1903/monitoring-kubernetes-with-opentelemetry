apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.name }}
  namespace: {{ .Release.Namespace }}
data:
{{- range $teamName, $teamInfo := .Values.newrelic }}
  {{ $teamName }}: {{ $teamInfo.licenseKey | b64enc }}
{{- end -}}
